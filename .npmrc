# Default to public npm
registry=https://registry.npmjs.org/

# Send only your scope to GitHub Packages
@bijikyu:registry=https://npm.pkg.github.com/

# Auth token for GitHub Packages (store it as a Replit Secret)
# Replit: Tools → Secrets → add key GITHUB_NODE_TOKEN with a PAT that has read:packages (and write:packages if publishing)
# Do NOT commit tokens to git.
 //npm.pkg.github.com/:_authToken=${GITHUB_NODE_TOKEN}